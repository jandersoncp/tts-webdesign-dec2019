// Step 1: define a variable that stores the number of cups that a person has consumed
// Step 2: use an if else statement
// if the person has had less that 3 cups, log "Yes, I'll take another cup of coffee"
// else log a message that says "I think I'm ok for now"
// Step 3: use	console.log() to log the above message to the console with the result
		 
var cups = 1;

if (cups < 3) {
	console.log("Yes, I'll take another cup of coffee");
} else {
	console.log("I think I'm ok for now.");
}